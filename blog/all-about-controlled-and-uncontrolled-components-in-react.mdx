---
title: 'Руководство по работе с формами и полями ввода в React'
date: '2022-02-23'
---

import Gist from 'react-gist';

<p>Все о контролируемых и неконтролируемых компонентах в React</p>

<p class="mt-8 mb-8">
  <span class="font-bold">Хочу сразу предупредить</span>. Перевод статьи не полный. Я не
  стал использовать главы где автор описывал "Перенаправление рефов". Если интересно
  почитать, то ссылка на оригинал в конце статьи😉
</p>

<p class="mt-8 mb-8">
  Работая с формами в React, вы столкнетесь с такими причудливыми терминами, как
  “Контролируемые” и “Неконтролируемые” компоненты. Моя цель в этой статье - изложить эти
  причудливые термины на простом языке.
</p>

<p class="mt-8 mb-8 font-bold text-4xl text-sky-600">Ref и DOM</p>

<p class="mt-8 mb-8">
  <span class="font-bold">ref</span> - это атрибут, который по умолчанию присутствует во
  всех входных элементах. Они позволяют нам получать доступ к другим элементам DOM и
  работать с ними.
</p>

<p class="mt-8 mb-8">
  Чтобы понять, что может сделать <span class="font-bold">ref</span>, давайте посмотрим,
  как используются формы. Если у вас есть какой-либо опыт работы с React, вы будете знать,
  что такое <span class="font-bold">useState</span> и как он может работать с полями
  формы. Для других давайте рассмотрим этот небольшой пример:
</p>

<div>
  <Gist id="d599dc550cc34af7c489550492379a3c" />
</div>

<p class="mt-8 mb-8">
  Здесь мы обновляем значение имени пользователя при каждом нажатии клавиши, используя
  обработчик события <span class="font-bold">onChange</span>, присутствующий при вводе, и
  сохраняем его в переменной name. Мы передаем это состояние обратно во входные данные,
  используя атрибут <span class="font-bold">value</span>. Затем мы используем обработчик
  отправки формы, чтобы использовать значение переменной и, наконец, очистить входные
  данные. Это прекрасный пример “контролируемого” поля ввода/компонента.
</p>

<p class="mt-8 mb-8">
  Здесь мы обновляем значение имени пользователя при каждом нажатии клавиши, используя
  обработчик события <span class="font-bold">onChange</span>, присутствующий при вводе, и
  сохраняем его в переменной name. Мы передаем это состояние обратно во входные данные,
  используя атрибут <span class="font-bold">value</span>. Затем мы используем обработчик
  отправки формы, чтобы использовать значение переменной и, наконец, очистить входные
  данные. Это прекрасный пример “контролируемого” поля ввода/компонента.
</p>

<p class="mt-8 mb-8">
  Это идеальный способ управлять состоянием формы. Но обновление состояния, то есть{' '}
  <span class="font-bold">name</span> при каждом нажатии клавиши, неэффективно, когда нам
  нужно только состояние во время отправки формы.
</p>

<p class="mt-8 mb-8">
  Это то место, где <span class="font-bold">ref</span> вступает в игру😉
</p>

<p class="mt-8 mb-8 font-bold text-4xl text-sky-600">Как работает ref?</p>

<p class="mt-8 mb-8">
  <span class="font-bold">ref</span> устанавливает соединение между HTML-элементом,
  который отображается в DOM, и кодом JavaScript. Мы делаем это с помощью хука –{' '}
  <span class="font-bold">useRef</span>.
</p>

<div class="mt-8 mb-8">
  <span class="font-bold">Примечание:</span>
  <ul>
    <li>• React.createRef (React 16.3)</li>
    <li>• useRef() хук (доступно с React 16.8)</li>
  </ul>
</div>

<p class="mt-8 mb-8">
  Как и все хуки, <span class="font-bold">useRef</span> следует всем правилам реактивных
  хуков. Он принимает значение по умолчанию, с помощью которого мы можем его
  инициализировать, но это необязательно. Он возвращает значение, которое позволит нам
  работать с этой ссылкой позже, как только мы подключим ее к элементу HTML DOM. Для
  подключения мы будем использовать встроенный атрибут/проп с именем{' '}
  <span class="font-bold">ref</span>, который присутствует в каждом элементе HTML DOM.
</p>

<div>
  <Gist id="3bd4d839b38991d650f3115ed1083777" />
</div>

<p class="mt-8 mb-8">
  Поскольку вы можете подключить <span class="font-bold">ref</span> к любому элементу HTML
  DOM, это не значит, что вы должны это делать. Используйте{' '}
  <span class="font-bold">ref</span> в основном с формами.
</p>

<p class="mt-8 mb-8">
  В конце этого <span class="font-bold">nameRef</span> фактически будет содержать полный
  элемент DOM в его текущем свойстве. Если мы попробуем вывести в консоль{' '}
  <span class="font-bold">nameRef.current</span>, мы увидим, что у нас есть весь элемент
  HTML DOM:
</p>

<div>
  <Gist id="9ec60036270abdcc7f07dd3d307d02ae" />
</div>

<p class="mt-8 mb-8">
  <span class="font-bold">Важное примечание:</span> React настоятельно не рекомендует
  манипулировать DOM императивно. Пользуйтесь хуком{' '}
  <span class="font-bold">useState</span> для управлением DOM. Это является идеальным
  способом выполнением задач. Варианты использования <span class="font-bold">useRef</span>{' '}
  различны и не должны быть вашим основным решением для манипулирования DOM.
</p>

<p class="mt-8 mb-8 font-bold text-4xl text-sky-600">Когда использовать ref?</p>

<p class="mt-8 mb-8">
  Идеальное место для использования <span class="font-bold">ref</span> - это когда вы
  хотите выполнять действия, доступные только для чтения. Иногда у вас будут случаи, когда
  вы хотите прочитать значение, а не изменять его. Если это так, вы можете избежать
  использования <span class="font-bold">useState</span> и воспользоваться{' '}
  <span class="font-bold">useRef</span>.
</p>

<div class="mt-8 mb-8">
  <p class="mt-4 mb-4">Ситуации, в которых использование рефов является оправданным:</p>
  <ul>
    <li>• Управление фокусом, выделение текста или воспроизведение медиа.</li>
    <li>• Императивный вызов анимаций.</li>
    <li>• Интеграция со сторонними DOM-библиотеками.</li>
  </ul>
</div>

<p class="mt-8 mb-8">
  Избегайте использования рефов в ситуациях, когда задачу можно решить декларативным
  способом.
</p>

<p class="mt-8 mb-8 font-bold text-4xl text-sky-600">Пример использования ref</p>

<p class="mt-8 mb-8">
  Рассмотрим вариант использования, в котором вы хотите установить focus на input элементе
  DOM при возникновении ошибки во время отправки формы. Мы можем сделать это, используя{' '}
  <span class="font-bold">ref</span>.
</p>

<p class="mt-8 mb-8">
  В приведенном ниже примере, если пользователь попытается отправить форму без ввода, это
  будет недопустимое действие отправки, и форма будет иметь focus.
</p>

<div>
  <Gist id="c1789eeb12b256834114c6c1dd90c274" />
</div>

<p class="mt-8 mb-8 font-bold text-4xl text-sky-600">
  Контролируемые компоненты vs Неконтролируемые компоненты
</p>

<p class="mt-8 mb-8">
  Все вышеперечисленное, о чем вы только что узнали, было не чем иным, как работой с
  “неконтролируемыми” компонентами. Под неконтролируемым это означает, что вы императивно
  обрабатываете значение/состояние и не используете React для этого.
</p>

<p class="mt-8 mb-8">
  С “контролируемыми” компонентами и полями ввода вы используете состояние для чтения,
  записи и обновления состояний компонентов. Это происходит, когда вы используете{' '}
  <span class="font-bold">useState</span> или <span class="font-bold">useReducer</span>{' '}
  для создания состояния, а затем привязываете это состояние к вашему компоненту/полю
  ввода. Как только вы это сделаете, оно станет “контролируемым” полем ввода/компонентом.
</p>

<p class="mt-8 mb-8 font-bold text-4xl text-sky-600">defaultValue vs value</p>

<p class="mt-8 mb-8">
  Вы не можете использовать атрибут <span class="font-bold">value</span> для
  неконтролируемых компонентов/полей формы. Если вы хотите присвоить значение по умолчанию
  неконтролируемому полю формы, используйте значение по{' '}
  <span class="font-bold">defaultValue</span>.
</p>

<div>
  <Gist id="4196536a499c8a65294243b5a5de6ae0" />
</div>

<p class="mt-8 mb-8">
  И вот мы подошли к концу. Попробуй поиграть и испачкать руки! Я надеюсь, что вы нашли
  это полезным. Спасибо вам за чтение.
</p>

<p class="mt-8 mb-8 ">
  Автор: <span className="text-sky-600">Aditya Tyagi</span>
</p>
<div>
  Ссылка на оригинал:{' '}
  <a
    className="text-sky-600"
    href="https://betterprogramming.pub/a-guide-to-working-with-forms-and-input-fields-in-react-403d64aaedf3"
    target="_blank">
    betterprogramming.pub
  </a>
</div>
